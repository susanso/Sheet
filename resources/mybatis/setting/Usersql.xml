<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sheet">
	<insert id="insertUser" parameterType="userdto">
		INSERT INTO sheet.user_info(userId, userPwd, userName) values(#{userId}, #{userPwd}, #{userName});
	</insert>
	
	<select id = "getPwd" parameterType="HashMap" resultType = "int">
		SELECT count(*) FROM sheet.user_info WHERE userId=#{id} and userPwd=#{pwd};
	</select>
	
	<select id = "getID" parameterType = "HashMap" resultType = "int">
		SELECT count(*) FROM sheet.user_info WHERE userID=#{id};
	</select>
</mapper>